# 부하 측정 기록

재현 시 주의: 브랜치를 왕복하며 측정하는 동안에는 `scripts/perf/` 를 **untracked 로 두어야 한다**
(`git switch` 가 untracked 파일은 건드리지 않는다). 측정이 끝난 뒤에 커밋한다.

## 측정 환경

| 항목 | 값 |
|---|---|
| API | Docker Compose, `localhost:8001` → 컨테이너 8000, uvicorn 워커 **1개** |
| 핸들러 | 전부 sync `def` → anyio threadpool (토큰 **40**) |
| MySQL | 원격 Oracle Cloud VM (WAN 경유), `max_connections` 151 |
| 부하 생성 | Locust 2.46.2 + geventhttpclient 2.3.9, `FastHttpUser` |
| 위치 | Locust 와 API 가 같은 노트북(물리 코어 16). CPU 경합 있음 |
| 인코딩 | `PYTHONUTF8=1` 필수 — 없으면 locust 가 `pyproject.toml`(한글 주석)을 cp949 로 읽다 죽는다 |

---

## 1단계 — `/health` 대조군 (2026-08-17)

DB 를 타지 않는 경로. 여기서 나온 RPS 가 이후 모든 수치의 해석 기준이다.
커넥션 풀과 무관하므로 브랜치 왕복 없이 1회만 측정.

### 계단식 (단일 프로세스, 각 30초)

| 동시 사용자 | RPS(평균) | p50 | p95 | p99 | 실패 |
|---:|---:|---:|---:|---:|---:|
| 1 | 835.7 | 1.0 | 1.0 | 2.0 | 0 |
| 5 | 1373.5 | 1.8 | 4.0 | 4.9 | 0 |
| 10 | 1649.7 | 3.1 | 6.5 | 7.6 | 0 |
| 20 | 1742.1 | 4.6 | 11.6 | 14.0 | 0 |
| 40 | 1781.0 | 6.0 | 21.7 | 25.9 | 0 |
| 80 | 1792.2 | 9.1 | 40.9 | 49.3 | 0 |
| 160 | 1790.0 | 11.6 | 77.8 | 95.1 | 0 |

RPS 는 20 사용자부터 평평(1742 → 1790, +2.8%)한데 p95 는 선형 상승(11.6 → 77.8).
**교과서적 포화 패턴** — 포화 후 동시성 추가는 처리량이 아니라 큐 대기만 늘린다.
포화 시작점 ≈ 동시 **10~20**.

### 천장 귀속 확인 (master + worker 4, 160 사용자 60초)

| 방식 | RPS | p50 | p95 | max |
|---|---:|---:|---:|---:|
| 단일 프로세스 | 1790.0 | 11.6 | 77.8 | — |
| master+worker 4 | **1787.1** | **89** | **100** | 210 |

- **RPS 가 동일 → 천장 1790 은 Locust 가 아니라 API 측.** 부하 생성기는 결백하다.
- 리틀의 법칙 정확히 성립: `160 ÷ 1787 = 89.5ms` = 실측 p50 89ms.
- **단일 프로세스는 지연을 과소보고한다** (p50 11.6 vs 89, p95 77.8 vs 100).
  그린렛이 CPU 를 기다리는 시간이 응답시간에 안 잡히기 때문.
  → **RPS 는 단일 프로세스로도 되지만, 지연 수치는 반드시 분산 모드로 잰다.**

### 결론

```
환경 천장 ≈ 1790 RPS  (uvicorn 1 워커 + Docker Desktop 포트포워딩 + 앱 CPU)
```

예측된 `/builds` 수치와 비교하면 헤드룸이 충분하다:

| | 예측 RPS | 천장 대비 |
|---|---:|---:|
| 풀 없음 (`40 / 0.2923s`) | 137 | 7.7% |
| 풀 P=10 (`10 / 0.0496s`) | 200 | 11% |
| 풀 P=40 | 806 | 45% |

→ **측정 환경이 DB 병목을 가리지 않는다.** 2단계 진행 가능.
(P=40 예측치 806 은 천장의 45% 라 여유가 줄어든다 — 그 구간 해석 시 주의)

---

## 2단계 — `/builds` 시나리오

### 데이터 현황 (2026-08-17)

`GET /builds` = 조합 3건. 항목 수는 2 / 4 / 4 개이고 작성자는 전부 동일 사용자다.
`stg_watchlist` 9행, `stg_products` 9행, `stg_price_history` 513행. 시나리오에 쓸 데이터는 충분하다.

### 단일 요청 지연 = 커넥션 점유시간 (풀 활성, P=10)

각 5회, `curl` 기준.

| 경로 | 지연 | ≈ RTT 환산 | 응답 |
|---|---:|---:|---:|
| `/health` | 2.5ms | 0 (DB 없음) | 15B |
| `/builds` | **52ms** | 1회 | 466B |
| `/builds/401` | 107ms | 2회 | 947B |
| `/builds/401/price-trend` | 167ms | 3회 | 2,140B |

**풀이 작동하는 증거**: 풀이 없으면 `/builds` = 개설 292ms + 쿼리 50ms ≈ 342ms 여야 한다.
실측 52ms → 커넥션 재사용 중. 3단계에서 `master` 로 가면 340ms 대로 뛰어야 정상이다(검산 지점).

### 포화점 예측 (P=10, 점유율 ≈100%)

핸들러 시간이 거의 전부 DB 대기라 커넥션 점유율이 ~100%다. 따라서 꺾임은 T(40)가 아니라
**P(10)에서 일어나야 한다**.

| 경로 | 예측 최대 RPS (`P / 점유시간`) | 예측 꺾임 | `/health` 천장 대비 |
|---|---:|---:|---:|
| `/builds` | 10 / 0.052 = **192** | 동시 10 | 11% |
| `/builds/401` | 10 / 0.107 = 93 | 동시 10 | 5% |
| `/builds/401/price-trend` | 10 / 0.160 = 62 | 동시 10 | 3% |

동시 10 초과부터 RPS 는 고정, p95 만 선형 상승해야 한다.

### 실측 — 예측이 깨졌다 (`--tags s1`, 단일 프로세스, 각 30초)

| users | RPS 실측 | RPS 예측(P=10) | p50 실측 | p50 예측 | 실패 |
|---:|---:|---:|---:|---:|---:|
| 1 | 18.8 | 19 | 50 | 52 | 0 |
| 2 | 35.2 | 38 | 50 | 52 | 0 |
| 5 | 85.2 | 96 | 50 | 52 | 0 |
| 8 | 145.6 | 154 | 50 | 52 | 0 |
| 10 | 182.2 | 192 | 50 | 52 | 0 |
| 13 | **228.9** | 192 | 50 | 68 | 0 |
| 16 | **241.3** | 192 | 51 | 83 | 0 |
| 20 | **264.3** | 192 | 54 | 104 | 0 |
| 30 | **353.1** | 192 | 59 | 156 | 0 |
| 40 | **373.2** | 192 | 65 | 208 | 0 |

10 을 넘어도 RPS 가 192 에 고정되지 않고 373 까지 올라갔다. 원인 **두 가지**를 찾았다.

### 원인 ① — 컨테이너 env 가 생성 시점에 굳어 있었다 (핵심)

```
Created:   2026-07-31T09:38:34Z   ← DB_POOL_SIZE=20 이 이때 박혔다
StartedAt: 2026-08-17T06:59:53Z   ← 오늘은 start 만 됐다
printenv DB_POOL_SIZE → 20
부하 중 MySQL Threads_connected → 20 (40초간 고정, 유휴 시에도 20)
```

**오늘 측정 전부가 P=10 이 아니라 P=20 에서 이뤄졌다.** `docker compose up -d` 는 이미 있는
컨테이너를 재사용하므로 `docker-compose.yml` 의 기본값 10 이 적용되지 않았다.

> **스윕 절차에 반드시 넣을 것**
> ```
> DB_POOL_SIZE=<P> docker compose up -d --force-recreate api
> docker compose exec -T api printenv DB_POOL_SIZE      # 반드시 확인
> curl --retry 15 --retry-delay 1 --retry-connrefused .../health
> SHOW STATUS LIKE 'Threads_connected'  → P 와 일치하는지 확인
> ```
> `restart` 로는 env 가 갱신되지 않는다. 이걸 몰랐다면 P 를 5/10/20/40 으로 바꿔도
> 전부 20 에서 측정돼 네 곡선이 같게 나왔고, **"풀 크기는 성능에 무관"** 이라는
> 정반대 결론이 나왔을 것이다.
>
> `mincached=P` 라 기동 시 커넥션을 미리 다 연다(개당 350ms). P=10 이면 약 3.5초간
> 요청을 받지 않는다 — 재생성 직후 `/health` 가 http 000 으로 실패하는 것을 확인했다.
> 스윕 스크립트에 기동 완료 폴링이 없으면 첫 계단이 통째로 실패한다.

### 원인 ② — 커넥션 점유율을 100% 로 잡은 게 틀렸다

DBUtils 는 결백하다. 통제 실험(컨테이너 내부, DBUtils 3.1.2):
`maxconnections=3` 풀에 12스레드가 동시에 대여를 시도 → 커넥션 **3개 유지**. 정상 동작.

P=20 으로 역산하면 모델이 맞는다:

```
점유시간 = 20 커넥션 ÷ 364 RPS = 55ms
점유율   = 55ms ÷ 109ms(40명 평균응답) ≈ 50%
```

| | 애초 가정 | 실측 교정 |
|---|---|---|
| 풀 크기 | 10 | **20** |
| 커넥션 점유시간 | 52ms (= 응답 전체) | **55ms** |
| 점유율 | ≈100% | **≈50%** |
| 포화 동시성 | P 명 | **2P 명** |
| 최대 RPS | P × 19 | **P × 18.2** |

응답의 절반은 커넥션 밖(HTTP·Docker 포트포워딩·직렬화·threadpool)에서 쓴다.
그래서 꺾임이 P 명이 아니라 **2P 명**에서 일어난다 — 40명까지 RPS 가 계속 오른 이유다.

참고 실측(컨테이너 내부, 커넥션 1개 재사용):

| | median |
|---|---:|
| 순수 RTT (`SELECT 1`) | **40.7ms** |
| connect+close (개설비용) | **350.1ms** |

7월 기록(RTT 49.6 / 개설 292.3, 호스트 측정)과 다르다. 개설비용이 커진 것은 Docker 경유와
네트워크 변동 탓으로 보인다. **`mysql_client.py` 의 `ping=0` 주석 "RTT 41ms" 는
오늘 실측 40.7ms 와 사실상 일치한다** — 7월에 남긴 "주석이 틀렸다"는 판단은 취소한다.

### 부산물: P=20 데이터 확보

위 표는 버리는 데이터가 아니라 **스윕의 P=20 지점**이다(`out/s1_P20_*.csv`).
P=20 에서 처리량 천장 ≈ **364~373 RPS**, 꺾임 ≈ 동시 40명.



## 5단계 — 풀 크기 스윕 (2026-08-17, `--tags s1`)

각 P 마다 `DB_POOL_SIZE=<P> docker compose up -d --force-recreate api` 후
`printenv` + `Threads_connected` 로 **실제 적용을 확인하고** 측정했다(전부 기대값 일치).

| P | 천장 RPS | 증가율 | 포화 동시성 | 실효 점유시간 `P/천장` | 최대 커넥션 수요 |
|---:|---:|---:|---:|---:|---:|
| 5 | 100 | — | 8 | 50.0ms | 5.5 = P |
| 10 | 209 | **+109%** | 15 | 47.8ms | 11.5 = P |
| 20 | **373** | **+78%** | 30 | 53.6ms | 20.5 = P |
| 40 | 384 | **+3%** | 60 | — | **21.1 ≪ P** |

### 결론: 최적 풀 크기 = **20**

곡선이 P=20 에서 평평해진다. P=40 은 커넥션을 2배 쓰고 처리량은 +3%(변동 범위 안)다.

동일 부하(동시 20명)에서의 p95 도 같은 지점에서 평평해진다 — 처리량과 지연이 상충하지 않는다.

| P | p95 @ 20명 |
|---:|---:|
| 5 | 188ms |
| 10 | 99ms |
| 20 | **82ms** |
| 40 | 83ms |

### 왜 20 인가 — 병목이 풀에서 threadpool 로 이동했다

```
anyio threadpool 토큰            = 40      (동시 실행 핸들러 상한)
커넥션 점유율                    ≈ 50%     (응답의 절반은 HTTP·Docker·직렬화)
→ 동시 커넥션 수요 상한          = 40 × 0.5 ≈ 20
```

실측이 그대로 재현한다: P=40 을 주고 동시 80명을 걸어도 커넥션 수요가 **21.1** 에서 멈춘다.
`P/H = 727 RPS` 예측이 -48% 빗나간 것은 모델이 틀린 게 아니라 **풀이 더 이상 병목이
아니라는 신호**다.

7월 예측("threadpool 40 이니 P>40 은 무의미")은 점유율 100% 가정 때문에 상한을 2배로
잡았다. 실측 점유율 50% 를 넣으면 **상한은 20** 이다.

**이 결론은 목표 RPS·p95 를 정하지 않아도 성립한다** — 20 을 넘으면 어떤 지표도 변하지 않는다.
반대로 커넥션을 아끼는 것이 목표라면 P 를 낮춰 처리량을 사는 트레이드오프가 가능하다
(P=10 이면 커넥션 절반으로 처리량 56%).

### 부수 발견 — 기동 대기에 `--retry-all-errors` 가 필요하다

`--retry-connrefused` 로는 부족하다. Docker 포트 프록시가 연결은 수락하고 앱이 아직
안 떠서 **빈 응답(curl exit 52)** 이 오는데, 이건 connection-refused 가 아니라 재시도되지
않는다. P=40 은 기동에 `40 × 350ms ≈ 14초` 가 걸린다.

```bash
curl -s --retry 40 --retry-delay 1 --retry-all-errors http://localhost:8001/health
```

---

## 3단계 — before: `master`, 풀 없음 (2026-08-17)

`git switch master` → `docker compose up -d --force-recreate api`.
검증: 유휴 시 api 커넥션 **0개**(풀 없음 확인), `scripts/perf/` 는 untracked 라 그대로 유지됐다.

`/builds` 단일 요청: **419ms** (5회, 390~428). 풀 있을 때 52ms 의 **8.1배**.
= 개설 350ms + RTT 41ms + 앱 9ms 구성과 일치한다.

계단식(`--tags s1`, 각 30초). 커넥션 제약이 없으므로 threadpool 40 이 유일한 상한이다.

| users | RPS | 예측(`40/0.41`) | 오차 | p50 | p95 | 커넥션 수요 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 2.3 | 2 | -2% | 402 | 417 | 1.0 |
| 5 | 10.8 | 12 | -10% | 400 | 410 | 4.4 |
| 10 | 23.5 | 24 | -2% | 400 | 410 | 9.6 |
| 20 | 47.2 | 48 | -1% | 391 | 410 | 19.3 |
| 40 | 86.1 | 95 | -10% | 396 | 455 | 35.3 |
| 60 | **93.6** | 98 | -4% | 404 | 655 | 38.4 ←포화 |

**p50 이 전 구간 400ms 로 일정하다** — 커넥션 개설이 완전히 지배해 부하와 무관하다.
60명에서 threadpool 40 이 포화(수요 38.4)되며 천장에 닿는다.

7월 예측 137 RPS 대비 실측 93.6 (-32%). 예측이 낙관적이었던 이유는 개설비용을 292ms 로
잡았기 때문이다 — 오늘 컨테이너 경유 실측은 350ms 다. `40/0.292=137` vs `40/0.410=98`.

---

## 4단계 — before / after 종합

| | 풀 없음 (`master`) | 풀 P=20 | 개선 |
|---|---:|---:|---:|
| `/builds` 단일 요청 | 419ms | **52ms** | **8.1배** |
| 처리량 천장 | 93.6 RPS | **373 RPS** | **4.0배** |
| p95 @ 동시 40명 | 455ms | **104ms** | **4.4배** |
| 유휴 커넥션 | 0 | 20 (상시) | — |
| 병목 | 커넥션 개설 350ms | uvicorn 워커 1개의 GIL | **이동 완료** |

### 병목 계층 (측정으로 확정된 순서)

```
① 커넥션 개설 350ms (요청의 90%)      → 풀 도입으로 제거 ✅
② 풀 크기 P<20                        → P=20 으로 제거 ✅
③ uvicorn 워커 1개의 GIL (~130% CPU)  ← 현재 병목
④ anyio threadpool 40                 → 도달하지 못함. 병목이 아니었다
⑤ MySQL max_connections 151           → 한참 멀다
```

### GIL 이 병목이라는 근거

같은 프로세스에서 두 워크로드가 **같은 CPU 천장**에 멈춘다(코어 16개 중 1.3개만 사용):

| 시나리오 | 동시 | RPS | 컨테이너 CPU | CPU/요청 |
|---|---:|---:|---:|---:|
| `/health` (DB 없음) | 40 | 1463 | **116%** | 0.79ms |
| `/builds` (DB 1회) | 40 | 378 | **130%** | 3.44ms |

처리량은 4배 차이인데 CPU 는 같다. 호스트는 8% 만 쓰므로 제약은 프로세스 **안**에 있다.
→ **threadpool T 를 늘려도 처리량은 오르지 않는다.** 다음 레버는 스레드가 아니라
**uvicorn 워커 수**(프로세스마다 GIL 이 따로)다. 단 워커마다 `init_pool()` 이 돌아
총 커넥션이 `워커 × P` 가 되므로 그때 MySQL 151 이 처음으로 실제 제약이 된다.

### P=20 의 근거는 두 갈래로 수렴한다

| 계산 | 값 |
|---|---:|
| threadpool 40 × 점유율 50% | 20 |
| CPU 천장 378 RPS × 점유시간 55ms | 21 |

두 경로가 같은 값을 주기 때문에 앞서 구분되지 않았다. 어느 쪽이든 **P=20**.

---

## 6단계 — 풀 파라미터 분리와 ping 비용 (2026-08-18)

재현: `docker compose exec -T api python - < scripts/perf/verify_pool_params.py`

### PooledDB 세 파라미터는 서로 다른 것을 통제한다

`mincached=2, maxcached=5, maxconnections=20` 으로 실측:

| 시점 | 커넥션 수 | 결정하는 파라미터 |
|---|---:|---|
| 기동 직후 | 2 | `mincached` |
| 동시 20 대여 중 | 20 | `maxconnections` |
| 전원 반납 후 | 5 | `maxcached` (초과 15개는 반납 시 `close`) |

**확장은 직렬이다.** PooledDB 가 `steady_connection()` 을 조건 락 안에서 호출하므로
새 커넥션이 1개당 350ms 씩 순서대로 열린다 — 약 **2.9 커넥션/초**.
그동안 모든 대여가 같은 락에서 대기한다.

### 채택한 설정과 그 근거

```
mincached=5,  maxcached=20,  maxconnections=20   (DB_POOL_SIZE=20, DB_POOL_MIN_CACHED=5)
```

`mincached` 를 낮추고 `maxcached=maxconnections` 로 두면 **램프업을 1회만** 낸다.
`maxcached` 를 낮추면(예: 5) 부하가 잦아들 때마다 15개가 닫혀 **버스트마다 램프업을 반복**한다.

실측 트레이드오프:

| | `mincached=20` | `mincached=5` (채택) |
|---|---:|---:|
| 기동 대기 | **8160ms** | **2081ms** |
| 유휴 커넥션 | 20 | **5** |
| 첫 부하 최악 지연 | 405ms | **5190ms** |
| 정상 상태 처리량 | 373 RPS | 373 RPS (동일) |
| 부하 후 유휴 | 20 | 20 |

첫 부하 꼬리 5190ms 는 램프업 그대로다 — `15 커넥션 × 350ms ≈ 5.25s`.
p50 은 110ms, p95 는 130ms 로 정상이고 **최악값만** 튄다. 재기동 후 첫 버스트에 1회 발생한다.
꼬리가 문제라면 `DB_POOL_MIN_CACHED` 를 올려 기동 시간과 맞바꾸면 된다.

램프업 실측(부하 40명 투입 시점부터):

```
t=0.0s  5개 → 1.7s  9개 → 3.3s  14개 → 5.0s  19개 → 6.6s  20개 (이후 유지)
부하 종료 +10초 뒤에도 20개 유지 ✅  (maxcached=20 이 닫지 않는다)
```

### ping=1 은 폐기했다 — 대여마다 왕복 1회

| | 대여+쿼리 median |
|---|---:|
| `ping=0` | **50.1ms** |
| `ping=1` | **100.0ms** |
| 차이 | **+49.9ms (정확히 2배)** |

`ping=1` 은 대여 시 `COM_PING` 을 보내 RTT 41ms 를 그대로 더한다. 드문 실패를 막기 위해
모든 요청을 2배 느리게 하는 거래라 채택하지 않았다.

**유휴 커넥션 staleness 대응은 미구현이다.** 검토한 대안:

- **예외 기반 재시도** (`OperationalError` 2006/2013 → 폐기 후 1회 재시도) — 정상 경로 비용 0.
  단 커넥션은 *서버가 문장을 실행한 뒤 응답 전에* 죽을 수 있고 `autocommit=True` 라
  재시도가 위험하다: 읽기와 파이프라인 쓰기(`ON DUPLICATE KEY UPDATE` 로 멱등)는 안전하지만
  API 쓰기(`create_build`, 회원가입)는 중복 생성된다. 읽기 한정 또는 쓰기 멱등화가 선행돼야 한다.
- **유휴 수명 제한 / TCP keepalive** — 낡기 전에 갈아치우거나 NAT 가 끊지 못하게 한다.
  2026-07-28 장애가 라우터·NAT 의 유휴 연결 끊김이었다면 요청당 비용 0 으로 해결된다.
  죽은 걸 감지하는 것보다 낡지 않게 하는 쪽이 싸다.

### ⚠ 스윕 측정 시에는 `mincached = maxconnections` 로 되돌릴 것

```bash
DB_POOL_SIZE=<P> DB_POOL_MIN_CACHED=<P> docker compose up -d --force-recreate api
```

`mincached < maxconnections` 이면 부하 중에 풀이 자라고, 확장이 커넥션당 350ms 직렬이라
초기 계단이 **부분 워밍 상태로 측정된다**. 실제로 `mincached=5` 로 40명을 걸었을 때
처리량이 373 → 325 RPS 로 내려갔다(첫 6.6초가 램프업에 쓰였다).

### 남은 것

- `price-trend`(점유시간 3배, 점유율 추정 73%)의 최적 P 는 29 부근으로 **추정**된다 — 미측정.
  실제 트래픽이 섞이면 20 이 전체 최적이라는 보장은 없다.
- 목표 RPS·p95 미정. 단 P=20 결론은 목표와 무관하게 성립한다(20 초과 시 어떤 지표도 변화 없음).
- **기본값 20 은 2026-08-18 에 반영했다** — `docker-compose.yml`, `mysql_client.py`, `.env.example`
  세 곳. 당초 우려했던 "유휴 커넥션 20개" 는 `mincached=5` 로 분리해 상시 5개로 줄였다(6단계).
- 유휴 커넥션 staleness 대응(예외 기반 재시도 또는 수명 제한)은 **미구현** — 6단계 참고.
- 워커 수 최적화 미착수. 병목이 GIL 로 확정됐으니 진짜 다음 레버지만 "풀 최적값" 범위를 넘는다.
  검증은 코드 변경 없이 compose 오버라이드 파일로 `--workers 2` 를 재면 된다.

